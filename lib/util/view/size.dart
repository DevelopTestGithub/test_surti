
// Used for sending widht/height data
// HasData paramater is used for optional sizes
class SizeSquare {
  final double width , height;
  final bool hasData;
  const SizeSquare(this.width, this.height, {this.hasData = true});
}
